import PropTypes from "prop-types";
import StyledText from "./styled-text";

const Text = (props) => {
  return <StyledText {...props} />;
};

Text.propTypes = {};

Text.defaultProps = {};

export default Text;
