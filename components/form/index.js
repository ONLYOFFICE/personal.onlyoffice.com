import React from "react";
import StyledForm from "./styled-form";

const Form = (props) => {
  return <StyledForm {...props} />;
};

Form.propTypes = {};

Form.defaultProps = {};

export default Form;
