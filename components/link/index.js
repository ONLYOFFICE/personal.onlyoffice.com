import PropTypes from "prop-types";
import StyledLink from "./styled-link";

const Link = (props) => {
  return <StyledLink {...props} />;
};

Link.propTypes = {};

Link.defaultProps = {};

export default Link;
